import React from "react";

// Minimal placeholder toaster so the app compiles. Extend with real toast logic later.
export function Toaster() {
  return <div className="fixed inset-x-0 bottom-0 pointer-events-none" />;
}
